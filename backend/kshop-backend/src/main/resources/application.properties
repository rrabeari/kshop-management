spring.application.name=kshop-backend

# =========================================================
# DATABASE - PostgreSQL LOCAL
# =========================================================

spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/kshop
spring.datasource.username=postgres
spring.datasource.password=Hasimpitiavana#28

# =========================================================
# JPA / HIBERNATE
# =========================================================

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# =========================================================
# SERVER
# =========================================================

server.port=8080

# =========================================================
# JWT
# =========================================================

app.jwt.secret=KShopDevSecretKeyForJwtAuthentication2026VeryLongSecretKey
app.jwt.expiration=86400000